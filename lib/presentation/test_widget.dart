import 'package:flutter/material.dart';

class NiceWidget extends StatelessWidget {
  const NiceWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("t",style: TextStyle(color: Colors.black)),);
  }
}
